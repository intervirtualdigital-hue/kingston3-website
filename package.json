{
  "name": "kingston3-consulting",
  "version": "1.0.0",
  "description": "Kingston 3 Consulting — Premium narrative strategy website",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "serve": "npx http-server . -p 3000 --silent"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "http-server": "^14.1.1"
  }
}
